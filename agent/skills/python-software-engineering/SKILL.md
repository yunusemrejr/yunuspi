---
name: python-software-engineering
description: "Engineer Python backends, desktop apps, ML pipelines and Linux tools with clear runtime and dependency boundaries."
---

# Python Software Engineering

Use for Python implementation and review across services, CLI tools, desktop applications and ML. Domain skills own statistical validity or UI taste; this skill owns Python runtime and software structure.

## Working method

- Inspect pyproject, lock/environment, interpreter and package/import layout.
- Keep parsing, effects and domain logic separable without unnecessary layers.
- Choose synchronous, async, threaded or process execution from the actual workload.
- Test boundary errors and representative runtime behavior in the intended environment.

Read [patterns and examples](references/patterns.md) for the relevant implementation mode; do not load unrelated modes. Inspect the actual runtime, project conventions and constraints before choosing syntax, dependencies or deployment steps. User instructions take precedence; this skill adds no authority to change external systems.

## Evidence and completion

Use a small representative case and the relevant failure case to check the result. Report what was executed, what remains unverified, and any material compatibility assumption. Do not invent measured outcomes or treat reading this guide as verification.
