id: ts-unnecessary-array-from
language: typescript
severity: warning
message: "Array.from(iterable) in for-of - iterate the iterable directly"
metadata:
  weight: 2
  category: slop
rule:
  pattern: "for (const $X of Array.from($ITER)) $$$BODY"
