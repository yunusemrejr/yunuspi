id: prefer-array-flat-map
language: TypeScript
severity: warning
message: "Use .flatMap() instead of .map().flat() — single pass, more efficient"
note: |
  arr.map(fn).flat() is two array traversals. arr.flatMap(fn) does the same in one.
rule:
  pattern: "$ARR.map($FN).flat()"
