id: prefer-date-now-js
language: JavaScript
severity: hint
message: "Use Date.now() instead of new Date().getTime()"
note: |
  Date.now() is simpler and avoids allocating a Date object.
rule:
  any:
    - pattern: "new Date().getTime()"
    - pattern: "new Date().valueOf()"
