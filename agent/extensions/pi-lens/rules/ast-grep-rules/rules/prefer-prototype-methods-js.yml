id: prefer-prototype-methods-js
language: JavaScript
severity: warning
message: "Use Object.hasOwn() instead of .hasOwnProperty()"
note: |
  .hasOwnProperty can be shadowed. Object.hasOwn(obj, key) is safe.
rule:
  pattern: "$OBJ.hasOwnProperty($$$ARGS)"
