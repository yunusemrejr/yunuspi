id: nested-ternary
language: TypeScript
message: "Nested ternary expressions are hard to read — use if/else or switch"
severity: warning
note: |
  Nested ternaries reduce readability. Extract to a function or use if/else.
rule:
  kind: ternary_expression
  has:
    kind: ternary_expression
    # catch nesting in either branch even when parenthesized (a ? (b ? c : d) : e)
    stopBy: end
