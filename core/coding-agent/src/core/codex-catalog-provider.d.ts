import type { Provider } from "@yunuspi/ai";
/** Discover visible models from the configured Codex account, without inference. */
export declare function withOpenAICodexCatalog(provider: Provider): Provider;
