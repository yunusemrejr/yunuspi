{
  "name": "yunuspi",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.19.0" },
  "devDependencies": { "htmlparser2": "9.1.0", "yaml": "2.8.3" },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check:public": "node scripts/check-public.mjs",
    "prepare:hooks": "git config core.hooksPath .githooks"
  }
}
